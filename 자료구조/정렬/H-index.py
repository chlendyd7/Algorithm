def solution(citations):
    citations.sort(reverse=True)
    for i, citations in enumerate(start=1):
        pass
    answer = max(map(min, enumerate(citations, start=1)))
    
    return answer
print(solution([3, 0, 6, 1, 5]))